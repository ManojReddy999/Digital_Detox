package com.example.digitalwellbeing.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors - Pure black and white
val Black = Color(0xFF000000)
val White = Color(0xFFFFFFFF)

// Background shades - Sophisticated grays
val Background = Color(0xFFF8F8F8)  // Slightly warmer than pure white
val Surface = Color(0xFFFFFFFF)
val CardBackground = Color(0xFFFFFFFF)  // Pure white cards on gray background for depth

// Text colors - Better hierarchy
val TextPrimary = Color(0xFF0A0A0A)  // Slightly softer than pure black
val TextSecondary = Color(0xFF6B6B6B)  // Improved contrast
val TextTertiary = Color(0xFF9E9E9E)

// Borders and dividers - Subtle separation
val Border = Color(0xFFEDEDED)  // More visible than before
val Divider = Color(0xFFE5E5E5)
val DividerLight = Color(0xFFF5F5F5)

// Shadows - For depth and elevation
val ShadowLight = Color(0x08000000)  // Very subtle shadow
val ShadowMedium = Color(0x10000000)  // Slightly stronger

// Interactive states
val PressedOverlay = Color(0x08000000)  // Overlay when pressed
val HoverOverlay = Color(0x04000000)  // Subtle hover effect
